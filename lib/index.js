/* jshint esnext:true */
import { FlagSet, StringFlag, BoolFlag, NumberFlag } from './flags';
import Command from './command';

const exp = {
    FlagSet,
    StringFlag,
    BoolFlag,
    NumberFlag,
    Command
};

export default exp;
